The app builds an agent and stops. It opens no socket, writes no row, and calls no model. Your own app runs it.
The Sync AI Tools Job now leaves a tool that a Git repository supplied to the repository sync.
A repository sync no longer fails when a tool registers again under the same name.
