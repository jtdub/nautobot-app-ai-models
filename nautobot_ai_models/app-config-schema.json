{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/jtdub/nautobot-app-ai-models/develop/nautobot_ai_models/app-config-schema.json",
    "$comment": "TBD: Update $id, replace `develop` with the future release tag",
    "type": "object",
    "properties": {
        "new_tools_enabled": {
            "type": "boolean",
            "default": true
        },
        "disable_on_definition_change": {
            "type": "boolean",
            "default": false
        },
        "checkpoint_retention_days": {
            "type": "integer",
            "minimum": 1,
            "default": 30,
            "description": "How many days a finished agent thread's LangGraph checkpoints are kept. Those tables are the checkpointer's own and nothing cascades into them, so this is the only thing that removes them."
        }
    },
    "additionalProperties": false
}
